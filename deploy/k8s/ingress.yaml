apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: iamd-ingress
  namespace: iamd-demo
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cop-dashboard
                port:
                  number: 8080
